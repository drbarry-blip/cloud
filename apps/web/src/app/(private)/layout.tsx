// No analytics, session recording, or third-party scripts beyond bot protection
// in this group. Users may paste sensitive text on these pages.
export default function PrivateLayout({ children }: { children: React.ReactNode }) {
  return children;
}
